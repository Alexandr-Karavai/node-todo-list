<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="/">TODO List</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                {{#if isIndex}}
                    <a class="nav-item nav-link active" href="/">View task</a>
                {{else}}
                    <a class="nav-item nav-link" href="/">View task</a>
                {{/if}}

                {{#if isCreate}}
                    <a class="nav-item nav-link active" href="/create">Create task</a>
                {{else}}
                    <a class="nav-item nav-link" href="/create">Create task</a>
                {{/if}}
            </div>
        </div>
    </div>
</nav>